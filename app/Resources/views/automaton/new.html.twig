{% extends 'layout.html.twig' %}
{% form_theme edit_form 'bootstrap_3_layout.html.twig' %}

{% block content %}
    <h1>Automaton creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('automaton_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
